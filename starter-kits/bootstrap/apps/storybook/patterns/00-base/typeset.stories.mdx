import { Meta, Typeset, Subtitle } from '@storybook/addon-docs/blocks';
import { storage } from '@wingsuit-designsystem/pattern';
const fontFamilies = {};
const fontSizes = {};

<Meta title="Base/Typography"
parameters={{ viewMode: 'docs', previewTabs: { canvas: { hidden: true }}}}
/>

# Typography
<Subtitle>Choosed typefaces and font sizes</Subtitle>

<div>
{
  Object.keys(fontFamilies).map((key,i)=>{
    return (
      <div key={key}>
        <Subtitle><b>Font Family:</b> {fontFamilies[key][0]} ({key})</Subtitle>
        <Typeset fontFamily={key} fontSizes={Object.values(fontSizes)} />
      </div>
    )
  })
}
</div>
