{% embed "@templates/site-container.twig"
  with {
  has_spacer: true,
  language_select: pattern_preview('language_select'),
  primary_menu: pattern_preview('menu', {}, 'primary'),
  footer_menu: pattern_preview('menu', {}, 'footer'),
} %}
  {% block main %}
    {% set more %}
      {% set cells = [
        pattern_preview('card', {}, 'default'),
        pattern_preview('card', {}, 'default'),
        pattern_preview('card', {}, 'default')
        ] %}
      {% embed '@organisms/grid/grid.twig' with {config: 'equal', columns: 3, gutter: 'default', cells: cells} %}
      {% endembed %}
    {% endset %}
    {{ pattern_preview('article', {more: more}) }}
  {% endblock %}
{% endembed %}
