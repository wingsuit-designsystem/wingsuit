import { Meta, IconGallery, IconItem } from '@storybook/addon-docs';
export const iconLib = require('@wingsuit-designsystem/preset-icon/patterns/icon.wingsuit.yml');
export const icons = Object.keys(iconLib.default.getVariant('spritemap').getSetting('icon').options);


<Meta title="Foundations/Icons"/>

# Icons

## A collection of simply beautiful icons

When adding new icons keep in mind that by stripping `fill` inline attributes fron inside the svg file usually they can be styled using css rules.  This way an icon can serve in an color combination.

<IconGallery>
  {
    icons.map((icon)=>{
      const url = `images/spritemap.svg#${icon}`;
      return (
        <IconItem name={icon} key={icon}>
          <svg viewBox="0 0 500 500">
            <use xlinkHref={url}></use>
          </svg>
        </IconItem>
      )
    })
  }
</IconGallery>
