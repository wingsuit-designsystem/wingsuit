{#
/**
 * @file
 * Placeholder component.
 *
 * Use this component for prototyping images.
 * Configure image styles in placeholder.wingsuit.yml.
 *
 * Use variant image or background.
 */
#}
{#
  Defaults
#}
{% set variant = variant|default('image') %}

{% import '@ws-placeholder/_placeholder-macro.twig' as placeholder %}

{# Template #}
{% if variant == 'image' %}
  {{ placeholder.image(style, service, src) }}
{% elseif variant == 'background' %}
  {{ placeholder.background(style, background_classes, service) }}
{% endif %}
