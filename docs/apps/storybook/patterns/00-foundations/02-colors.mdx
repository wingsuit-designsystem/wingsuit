import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs';
export const tailwindFile = require('../../config/silo/tailwind.json');
export const colors = tailwindFile.tailwind.theme.colors;

<Meta title="Foundations/Colors" />
# Colors Matter

Describe your palette of great-looking, well-balanced colors.

<ColorPalette>
  {
    Object.keys(colors).map((key)=>{
      const color = typeof colors[key] === 'string' ? [colors[key]] : colors[key];
      const title = `.${key}`;
      return (
        <ColorItem key={key} title={title} colors={color} />
      )
    })
  }
</ColorPalette>
