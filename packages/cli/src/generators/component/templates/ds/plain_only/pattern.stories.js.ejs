import React from 'react';
import { RenderTwig } from '@wingsuit-designsystem/storybook';<% if (useDoc == 'mdx') { %>
import './index';
import mdx from './<%= name %>.mdx';
<% } %>

const template = require('./<%= name %>.twig');

export default {
  title: '<%= capitalizeCleanPatternType %>/<%= capitalizeName %>',<% if (useDoc == 'mdx') { %>
  parameters: {
    docs: {
      page: mdx,
    },
  },<% } %>
};

export const <%= camelCaseName %> = () => <RenderTwig data={template} />;
