import React from 'react';
import { RenderTwig } from '@wingsuit-designsystem/storybook';<% if (useDoc == 'mdx') { %>
import mdx from './<%= name %>.mdx';
<% } %>
const template = require('./<%= name %>.twig');

export default {
  title: '<%= cleanPatternType %>/<%= capitalizeName %>',<% if (useDoc == 'mdx') { %>
  parameters: {
    docs: {
      page: mdx,
    },
  },<% } %>
};

export const Demo = () => <RenderTwig data={template} />;
