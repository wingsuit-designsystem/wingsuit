import React from 'react';
import { RenderTwig } from '@wingsuit-designsystem/pattern-react';<% if (useDoc == 'mdx') { %>
import mdx from './<%= name %>.mdx';
<% } %>
const template = require('./<%= name %>.twig');

export default {
  title: '<%= cleanPatternType %>/<%= capitalizeName %>',<% if (useDoc == 'mdx') { %>
  parameters: {
    docs: {
      page: mdx,
    },
  },<% } %>
};

export const <%= camelCaseName %> = {
  name: 'Default',
  args: {
    data: template
  }
}


