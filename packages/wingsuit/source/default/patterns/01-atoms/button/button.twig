{% set tag_name = tag_name|default('div') %}
{% set attributes = attributes|default(create_attribute()) %}
{% set svg_size = svg_size|default('default') %}

{% if url is not empty %}
  {% set attributes = attributes.setAttribute('href', url) %}
  {% set tag_name = 'a' %}
{% endif %}
{% set icon_size = ([
  icon_size == 'small' ? 'w-8 h-8',
  icon_size == 'medium' ? 'w-10 h-10',
  icon_size == 'large' ? 'w-12 h-12',
]) | sort | join(' ') | trim %}

<{{ tag_name }} {{ attributes.addClass([
  'flex items-center inline-flex border-0 py-1 px-2 focus:outline-none rounded',
  variant == 'default' ? 'bg-'~ color ~' text-' ~ text_color ~' hover:bg-' ~ color ~ '-600',
  variant == 'outline' ? 'border border-'~ color ~' text-' ~ text_color,
  expanded == 'full' ? 'w-full',
  size == 'small' ? 'text-xs',
  size == 'medium' ? 'text-base',
  size == 'large' ? 'text-xl',
  svg_alignment == 'left' ? 'flex-row-reverse',
] | sort | join(' ') | trim) }}>
{{ text }}
  {% if svg is not empty %}
    {% include "@atoms/svg/_svg--icon.twig" with {
      icon: svg,
      fill_color: 'currentColor',
      size: icon_size,
    } %}
  {% endif %}
</{{ tag_name }}>
