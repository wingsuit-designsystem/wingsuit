import { Meta, Story, Preview, Title, Subtitle, Primary, Heading, Subheading } from '@storybook/addon-docs/blocks';

import { PatternDoc, PatternLoad } from '@wingsuit-designsystem/storybook';
import { storage } from '@wingsuit-designsystem/pattern';
import { Source } from '@storybook/components';
import dedent from 'ts-dedent';

<PatternLoad patternId="grid">
  {(pattern) => (
  <>
    <Title />
    <Subtitle />
    <Description />
    <Heading>
      Usage
    </Heading>
    <Source format={false} language="twig" dark code={dedent`
      {% set items = [
        'Item 1',
        'Item 2'
      ] %}
      {% include "@organisms/grid/grid.twig" with {
        attributes: create_attribute(),
        items: items,
        columns_count: 2,
        columns_config: 'equal',
        gutter: 'default'
      } only %}
    `} />
    <Heading>
      Column widths
    </Heading>
    <p>
      The component ships with most common columns widths configurations like 50x50 or 33x66.
      If you need additional configuration you can extends the columns_configuration inside the grid.wingsuit.yml.
    </p>
    <Source format={false} language="yml" dark code={dedent`
        columns:
          2: << Columns count
            '66x33': << Name
              1: 'w-full md:w-2/3' << Every first column
              2: 'w-full md:w-1/3' << Every second column
          3:
            equal:
              default: 'w-full md:w-1/3' << Default for every other column
          `}/>
    <Heading>
      Gutter
    </Heading>
    <p>
      To configure the spacing between the columns use the gutter configuration in the grid.wingsuit.yml.
    </p>
    <Source format={false} language="yml" dark code={dedent`
      gutter:
        ...
        default: << Name of
          container: -mx-6 -md:mx-25
          cell: px-6 md:px-25 mb-6 last:mb-0
        ...
          `}/>
    <PatternDoc pattern={pattern} showInclude={false} includePrimary={true}/>
  </>
  )}
</PatternLoad>
