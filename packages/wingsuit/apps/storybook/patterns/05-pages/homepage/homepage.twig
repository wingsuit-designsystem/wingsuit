{% embed "@templates/site-container.twig"
  with {
  primary_menu: pattern_preview('menu', 'primary'),
  footer_menu: pattern_preview('menu', 'footer')
} %}
  {% block main %}
    <div class="main-content w-full py-10 mt-20">
      <div class="max-w-container px-4 m-auto">
        <div class="text-center w-1/2 m-auto pb-8 text-gray-dark">
          {{ "Lorem ipsum is placeholder text commonly used in the graphic, print, and publishing industries for previewing layouts and visual mockups." }}
        </div>
        {% embed '@organisms/grid/grid.twig' with {config_3: 'equal', columns: 3, gap: true, wrapper: false, width: 'large', padding_top: 'large', padding_bottom: 'large'} %}
          {% block column_1 %}
            {{ pattern_preview('card') }}
          {% endblock %}
          {% block column_2 %}
            {{ pattern_preview('card') }}
          {% endblock %}
          {% block column_3 %}
            {{ pattern_preview('card') }}
          {% endblock %}
        {% endembed %}
      </div>
    </div>
  {% endblock %}
{% endembed %}
